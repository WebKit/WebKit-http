/*
    This file is generated just to tell build scripts that JSNavigatorMediaDevices.h and
    JSNavigatorMediaDevices.cpp are created for NavigatorMediaDevices.idl, and thus
    prevent the build scripts from trying to generate JSNavigatorMediaDevices.h and
    JSNavigatorMediaDevices.cpp at every build. This file must not be tried to compile.
*/
